// var ip = require('ip');

module.exports = {
  email: {
    enabled: false,
    loggerEnabled: true,
    // loggerPath: paths.emailCache,
  },
  server: {
    host: 'http://localhost:6004',
  },
  log: 'debug',
};
